# Pinned environment for the supervised-clustering pipeline.
#
# Exact versions of the direct dependencies plus the numerical stack
# (numpy / numba / scipy / joblib) whose drift silently changes UMAP output.
#
# CRITICAL CONSTRAINT: numba requires numpy < 2.2, and UMAP and SHAP both run
# on numba. Do NOT let an install pull numpy >= 2.2 (e.g. `pip install imodels`
# upgrades numpy to 2.4 and breaks umap/shap). When adding a package, install
# it in a separate environment or check it does not move numpy/scikit-learn.
#
# Install into the anaconda env with:
#   python -m pip install -r requirements.txt

# Numerical stack (pinned tightly — drift here perturbs UMAP embeddings)
numpy==2.1.3
numba==0.61.0
scipy==1.17.1
joblib==1.5.3

# Core ML / pipeline
scikit-learn==1.6.1
pandas==2.2.3
lightgbm==4.6.0
shap==0.51.0
umap-learn==0.5.12
hdbscan==0.8.42
kneed==0.8.6

# Neural-network path (MLP + LRP)
torch==2.10.0
zennit==1.0.0

# Alternative attribution
lime==0.2.0.1
pacmap==0.9.1

# Data, plotting, config
ucimlrepo==0.0.7
matplotlib==3.10.0
seaborn==0.13.2
PyYAML==6.0.2
